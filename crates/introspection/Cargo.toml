[package]
name = "dsql-introspection"
version = "0.1.0"
edition = "2024"

[dependencies]
dsql-core = { workspace = true }
sqlx = { workspace = true, features = ["runtime-tokio", "tls-rustls-ring-webpki", "postgres", "derive"] }
thiserror = { workspace = true }
