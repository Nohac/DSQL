[package]
name = "dsql-project"
version = "0.1.0"
edition = "2024"

[dependencies]
dsql-core = { workspace = true }
facet = { workspace = true }
facet-toml = { workspace = true, features = ["serialize"] }
miette = { workspace = true, features = ["fancy"] }
