[package]
name = "dsql-project"
version = "0.1.0"
edition = "2024"

[dependencies]
dsql-core = { path = "../core" }
facet = "0.46.2"
facet-toml = { version = "0.46.0", features = ["serialize"] }
miette = { version = "7", features = ["fancy"] }
