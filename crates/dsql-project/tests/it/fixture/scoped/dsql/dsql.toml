database_url = "<database url>"
default_schema = "public"

[resolution.shared]
documents = ["queries/shared/**/*.dsql"]

[resolution.frontend]
documents = ["queries/frontend/**/*.dsql", "src/**/*.ts", "src/**/*.tsx"]
imports = ["shared"]

[lint]
unindexed_scan_severity = "warning"
