{
  "name": "tree-sitter-dsql",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tree-sitter generate",
    "check:surface": "node scripts/check-language-surface.mjs",
    "test:corpus": "node scripts/test-corpus.mjs",
    "check:captures": "node scripts/check-captures.mjs",
    "test:fixtures": "node scripts/check-fixtures.mjs",
    "test": "bun run check:surface && bun run check:captures && bun run test:corpus && bun run test:fixtures"
  }
}
